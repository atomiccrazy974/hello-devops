{
  "name": "hello-devops",
  "version": "1.0.0",
  "description": "Pipeline CI TP2",
  "main": "src/app.js",
  "scripts": {
    "test": "node --test --test-reporter=spec --test-reporter=junit --test-reporter-destination=stdout --test-reporter-destination=reports/junit.xml"
  }
}
